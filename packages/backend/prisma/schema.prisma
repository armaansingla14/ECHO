datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Event {
  id        Int      @id @default(autoincrement())
  timestamp DateTime
  source    String
  type      String
  text      String?
  meta      String?
}
